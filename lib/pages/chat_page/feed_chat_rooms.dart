import 'package:flutter/material.dart';

class MyFeedChatRoomList extends StatelessWidget {
  const MyFeedChatRoomList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("feed chat rooms"),
    );
  }
}
