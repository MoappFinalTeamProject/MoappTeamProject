import 'package:flutter/material.dart';

class MyMatchingChatRoomList extends StatelessWidget {
  const MyMatchingChatRoomList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("matching chat rooms"),
    );
    ;
  }
}
