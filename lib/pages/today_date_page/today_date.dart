import 'package:flutter/material.dart';

class TodayDatePage extends StatelessWidget {
  const TodayDatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Today Date Page"),
      ),
      body: Placeholder(),
    );
  }
}