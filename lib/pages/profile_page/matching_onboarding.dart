import 'package:flutter/material.dart';

class MatchingOnboarding extends StatelessWidget {
  const MatchingOnboarding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}